import styled from 'styled-components';

const WellcomeSection = styled.section`
    margin: 0px auto;
    padding: ${({theme}) => theme.spacing(16)} 0px;

    max-width: 1200px;
`;

export { WellcomeSection };
